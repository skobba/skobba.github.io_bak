<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ page.title | escape }}</title>
</head>
<body>
    <header>
        <h1>{{ site.title }}</h1>
    </header>
   
    <!-- <nav>
        <ul>
            {% for page in site.pages %}
            {% if page.data.title %}
                <li><a href="{{ page.url }}">{{ page.title }}</a></li>
            {% endif %}
            {% endfor %}
        </ul>
    </nav> -->

    <nav>
        <div>
            <div id="table-of-contents">
              {{ content | toc_only }}
            </div>
            <div id="markdown-content">
              {{ content | inject_anchors }}
            </div>
          </div>
    </nav>

    <main>
        {{ content }}  <!-- This is where the Markdown content is inserted -->
    </main>
    
    <footer>
        <p>&copy; {{ site.time | date: '%Y' }}</p>
    </footer>
</body>
</html>
